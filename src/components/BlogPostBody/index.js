import styled from "styled-components"

const BlogPostBody = styled.div`
  line-height: 1.4;
  padding: 16px 0;

  * > strong {
    font-size: larger;
    font-weight: bold;
  }
`

export default BlogPostBody
